---
phase: 02-runtime-core-and-safe-delegation
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - packages/octoclaw-runtime-core/src/workflow/index.ts
  - extensions/octoclaw-delegation/src/materialize/index.ts
  - extensions/octoclaw-delegation/src/brief/index.ts
files_modified_additional:
  - packages/octoclaw-policy/src/admission/index.ts
autonomous: true
requirements:
  - RT-01
  - SAFE-01
  - SAFE-02
gap_closure: true
must_haves:
  truths:
    - "The runtime-core workflow module loads under the repo's target Node ESM path and can advance an owned workflow without self-conflict."
    - "Runtime-core owns task materialization at workflow start instead of leaving task packets implicit."
    - "Delegated work carries idempotency, receipt, claim, and lease metadata before launch and passes through admission control."
  artifacts:
    - path: "packages/octoclaw-runtime-core/src/workflow/index.ts"
      provides: "Executable workflow ownership, task materialization, and valid single-owner transitions"
    - path: "extensions/octoclaw-delegation/src/materialize/index.ts"
      provides: "Delegated packet materialization with SAFE-01 and SAFE-02 metadata"
    - path: "extensions/octoclaw-delegation/src/brief/index.ts"
      provides: "Worker brief contract that carries delivery and ownership constraints"
    - path: "packages/octoclaw-policy/src/admission/index.ts"
      provides: "Admission decision API consumed before delegation launch"
  key_links:
    - from: "packages/octoclaw-runtime-core/src/workflow/index.ts"
      to: "packages/octoclaw-runtime-core/src/ack/index.ts"
      via: "ESM-safe import"
      pattern: "../ack/index"
    - from: "extensions/octoclaw-delegation/src/materialize/index.ts"
      to: "packages/octoclaw-policy/src/admission/index.ts"
      via: "evaluateAdmission() before launch"
      pattern: "evaluateAdmission"
---

<objective>
Close the executable ownership and delegation-safety gaps by making runtime-core materialize work correctly under Node ESM, fixing single-owner transitions, and pushing SAFE-01 metadata plus admission control into the delegation seam.

Purpose: Restore the missing end-to-end ownership and delegation guarantees required by RT-01, SAFE-01, and SAFE-02.
Output: An executable runtime-core workflow module and delegation materialization path that carries metadata and admission decisions before launch.
</objective>

<execution_context>
@/Users/guanbear/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/guanbear/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-core-and-safe-delegation/02-CONTEXT.md
@.planning/phases/02-runtime-core-and-safe-delegation/02-VERIFICATION.md
@.planning/phases/02-runtime-core-and-safe-delegation/02-02-SUMMARY.md

<interfaces>
From packages/octoclaw-runtime-core/src/workflow/index.ts:
```typescript
export interface RuntimeWorkflowState {
  requestId: string;
  taskId: string;
  flowId: string;
  ingressOrchestration: "queued" | "accepted" | "rejected";
  workflowOrchestration: "planned" | "running" | "waiting" | "completed" | "failed";
  reconcileOrRecovery: "idle" | "reconciling" | "recovering";
  deadlines: RuntimeDeadlines;
  claim: RuntimeClaim | null;
  outbox: DeliveryOutbox;
  ackLedger: AckLedger;
  scope: ScopeMetadata;
}

export interface StartWorkflowInput {
  requestId: string;
  taskId: string;
  flowId: string;
  claimOwner: string;
  leaseDurationMs: number;
  deadlineBudget: DeadlineBudgetInput;
  scope: ScopeMetadata;
}

export function startRuntimeWorkflow(input: StartWorkflowInput): RuntimeWorkflowState;
export function advanceWorkflowToRunning(state: RuntimeWorkflowState, nextClaimOwner: string): RuntimeWorkflowState;
```

From packages/octoclaw-policy/src/admission/index.ts:
```typescript
export interface AdmissionDecision {
  admission: "allow" | "defer" | "reject";
  queueBudget: number;
  reason: string;
}

export function evaluateAdmission(input: AdmissionInput): AdmissionDecision;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix runtime-core task materialization and single-owner workflow transitions</name>
  <files>packages/octoclaw-runtime-core/src/workflow/index.ts</files>
  <read_first>
    <file>packages/octoclaw-runtime-core/src/workflow/index.ts</file>
    <file>packages/octoclaw-runtime-core/src/ack/index.ts</file>
    <file>packages/octoclaw-runtime-core/src/tasks/claims.ts</file>
    <file>.planning/phases/02-runtime-core-and-safe-delegation/02-VERIFICATION.md</file>
  </read_first>
  <behavior>
    - Test 1: importing `packages/octoclaw-runtime-core/src/workflow/index.ts` under Node ESM succeeds without `ERR_UNSUPPORTED_DIR_IMPORT`.
    - Test 2: `startRuntimeWorkflow()` materializes explicit task-packet ownership data needed for workflow start.
    - Test 3: `advanceWorkflowToRunning()` preserves or renews the active owner claim without throwing `claim_owner_conflict` when the same owner advances the workflow.
  </behavior>
  <action>Refactor `packages/octoclaw-runtime-core/src/workflow/index.ts` to own task materialization explicitly per D-02 and the verification gap. Replace the unsupported `../ack` directory import with an ESM-safe file import. Add a task-materialization structure or helper on workflow start that records the task packet identity and ownership metadata rather than leaving materialization implicit. Update `advanceWorkflowToRunning()` so an already-owned workflow can move from `planned` to `running` when the same owner advances it, while still rejecting true conflicting owners. Keep ACK ledger, deadlines, claim state, and outbox ownership inside runtime-core instead of delegating those semantics elsewhere.</action>
  <acceptance_criteria>
    <criterion>`packages/octoclaw-runtime-core/src/workflow/index.ts` imports `../ack/index` or another explicit file path instead of `../ack`.</criterion>
    <criterion>`packages/octoclaw-runtime-core/src/workflow/index.ts` contains an explicit task materialization field, type, or helper for workflow start.</criterion>
    <criterion>`packages/octoclaw-runtime-core/src/workflow/index.ts` allows same-owner advancement without throwing `claim_owner_conflict`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import('./packages/octoclaw-runtime-core/src/workflow/index.ts').then(async (m)=>{const scope={readScope:[],writeScope:[],workspaceMode:'isolated_workspace'}; const state=m.startRuntimeWorkflow({requestId:'r1',taskId:'t1',flowId:'f1',claimOwner:'owner-a',leaseDurationMs:30000,deadlineBudget:{queueMs:1,startMs:1,progressMs:1,runtimeMs:1,deliveryMs:1},scope}); const next=m.advanceWorkflowToRunning(state,'owner-a'); if(next.workflowOrchestration!=='running') process.exit(2);}).catch(()=>process.exit(1));"</automated>
  </verify>
  <done>The workflow module loads cleanly and the runtime core owns executable task materialization plus correct same-owner state transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry SAFE-01 metadata and admission checks through delegation materialization</name>
  <files>extensions/octoclaw-delegation/src/materialize/index.ts, extensions/octoclaw-delegation/src/brief/index.ts, packages/octoclaw-policy/src/admission/index.ts</files>
  <read_first>
    <file>extensions/octoclaw-delegation/src/materialize/index.ts</file>
    <file>extensions/octoclaw-delegation/src/brief/index.ts</file>
    <file>packages/octoclaw-policy/src/admission/index.ts</file>
    <file>packages/octoclaw-contracts/src/deliveries.ts</file>
    <file>packages/octoclaw-contracts/src/events.ts</file>
    <file>.planning/phases/02-runtime-core-and-safe-delegation/02-VERIFICATION.md</file>
  </read_first>
  <behavior>
    - Test 1: delegated materialization returns idempotency, delivery receipt, claim owner, claim token, lease expiry, readScope, writeScope, and workspaceMode metadata.
    - Test 2: delegated materialization evaluates admission before launch and surfaces `allow`, `defer`, or `reject` in the returned structure.
    - Test 3: worker briefs include delivery and ownership constraints so downstream workers see the same safety contract.
  </behavior>
  <action>Extend `extensions/octoclaw-delegation/src/materialize/index.ts` so delegated packets include SAFE-01 metadata from D-02: idempotency key, delivery receipt ID, claim owner, claim token, lease expiry, and admission result alongside the existing `readScope`, `writeScope`, `workspaceMode`, and `writeScopeSummary`. Import and invoke `evaluateAdmission()` from `packages/octoclaw-policy/src/admission/index.ts` before returning launch-ready work, using queue budget, inflight count, capability status, workspace mode, and write-conflict inputs rather than launching blindly. Update `extensions/octoclaw-delegation/src/brief/index.ts` so worker constraints and done criteria explicitly mention ownership and delivery-receipt obligations. Only adjust `packages/octoclaw-policy/src/admission/index.ts` if needed to support the delegation input shape; preserve the Phase 2 shared-workspace serialization default from D-02.</action>
  <acceptance_criteria>
    <criterion>`extensions/octoclaw-delegation/src/materialize/index.ts` contains fields for idempotency, receipt, claim, lease, `readScope`, `writeScope`, and `workspaceMode`.</criterion>
    <criterion>`extensions/octoclaw-delegation/src/materialize/index.ts` imports or invokes `evaluateAdmission`.</criterion>
    <criterion>`extensions/octoclaw-delegation/src/brief/index.ts` mentions delivery receipt or ownership obligations in worker constraints or done criteria.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n "evaluateAdmission|idempotency|receipt|claimOwner|claimToken|lease|readScope|writeScope|workspaceMode" extensions/octoclaw-delegation/src/materialize/index.ts extensions/octoclaw-delegation/src/brief/index.ts packages/octoclaw-policy/src/admission/index.ts</automated>
  </verify>
  <done>Delegation launch data now carries SAFE-01 and SAFE-02 metadata and is filtered through admission control before work is launched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime workflow state → task launch | Ownership state crosses from internal orchestration to executable task materialization |
| delegation request → worker launch packet | Untrusted delegation inputs become launch-ready work if admission allows them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | D | packages/octoclaw-runtime-core/src/workflow/index.ts | mitigate | Preserve same-owner claim advancement and reject only true owner conflicts so valid work is not deadlocked |
| T-02-04-02 | T | extensions/octoclaw-delegation/src/materialize/index.ts | mitigate | Bind admission decisions plus idempotency, claim, and lease metadata into the launch packet before scheduling |
| T-02-04-03 | R | extensions/octoclaw-delegation/src/brief/index.ts | mitigate | Surface ownership and delivery-receipt obligations in worker briefs so execution expectations remain auditable |
</threat_model>

<verification>
- Run `node --input-type=module -e "import('./packages/octoclaw-runtime-core/src/workflow/index.ts').then(async (m)=>{const scope={readScope:[],writeScope:[],workspaceMode:'isolated_workspace'}; const state=m.startRuntimeWorkflow({requestId:'r1',taskId:'t1',flowId:'f1',claimOwner:'owner-a',leaseDurationMs:30000,deadlineBudget:{queueMs:1,startMs:1,progressMs:1,runtimeMs:1,deliveryMs:1},scope}); const next=m.advanceWorkflowToRunning(state,'owner-a'); if(next.workflowOrchestration!=='running') process.exit(2);}).catch(()=>process.exit(1));"`.
- Run `rg -n "evaluateAdmission|idempotency|receipt|claimOwner|claimToken|lease|readScope|writeScope|workspaceMode" extensions/octoclaw-delegation/src/materialize/index.ts extensions/octoclaw-delegation/src/brief/index.ts packages/octoclaw-policy/src/admission/index.ts`.
</verification>

<success_criteria>
- Runtime-core owns explicit task materialization and can advance an owned workflow without self-conflict.
- The workflow module loads under the target Node ESM path.
- Delegation packets carry SAFE-01 metadata and admission outcomes before launch while preserving SAFE-02 workspace conflict behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-core-and-safe-delegation/02-04-SUMMARY.md`
</output>
