---
phase: 03-native-substrate-and-operator-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts
  - extensions/octoclaw-runtime/src/plugin.ts
  - extensions/octoclaw-runtime/index.js
  - packages/octoclaw-contracts/src/schemas.ts
  - packages/octoclaw-contracts/src/artifacts.ts
  - tests/test_openclaw_taskflow_adapter.py
  - tests/test_octoclaw_runtime_extension.py
autonomous: true
requirements:
  - NATIVE-01
must_haves:
  truths:
    - "OpenClaw native task and flow APIs are the formal execution truth path for the runtime adapter."
    - "The shipped runtime wrapper no longer acts as the semantic truth owner; it delegates truth-path authority to the TypeScript-native adapter/plugin seam."
    - "Native truth payloads remain separate from projection/artifact/telemetry payloads."
  artifacts:
    - path: "extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts"
      provides: "Formal TypeScript-native adapter for OpenClaw bind/create/run truth operations"
    - path: "extensions/octoclaw-runtime/src/plugin.ts"
      provides: "Runtime plugin wiring that exposes the native-truth adapter and truth-bound workflow projection hooks"
    - path: "packages/octoclaw-contracts/src/artifacts.ts"
      provides: "Shared contract types for native truth/projection separation used by downstream surfaces"
  key_links:
    - from: "extensions/octoclaw-runtime/index.js"
      to: "extensions/octoclaw-runtime/src/plugin.ts"
      via: "formal runtime truth delegation"
      pattern: "createOctoClawRuntimePlugin|resolveRuntimePolicyDecision|createAdapter"
    - from: "extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts"
      to: "lib/openclaw_taskflow_runtime_helper.mjs"
      via: "native bind/create/run semantics"
      pattern: "bindSession|createManaged|runTask"
---

<objective>
Make the TypeScript-native adapter path the formal runtime truth seam for OpenClaw task and flow execution.

Purpose: Satisfy D-01 and D-02 by completing the cutover Phase 2 deferred, while preserving the truth/projection separation locked by D-05.
Output: A formal TS-native runtime adapter contract, shipped runtime wiring that delegates truth-path authority to it, and verification coverage proving the native path is authoritative.
</objective>

<execution_context>
@/Users/guanbear/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/guanbear/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-substrate-and-operator-surfaces/03-CONTEXT.md
@.planning/phases/03-native-substrate-and-operator-surfaces/03-RESEARCH.md
@.planning/phases/03-native-substrate-and-operator-surfaces/03-VALIDATION.md
@.planning/phases/02-runtime-core-and-safe-delegation/02-02-SUMMARY.md
@.planning/phases/02-runtime-core-and-safe-delegation/02-03-SUMMARY.md
@.planning/phases/02-runtime-core-and-safe-delegation/02-04-SUMMARY.md

<interfaces>
From `extensions/octoclaw-runtime/src/plugin.ts`:
```ts
export interface OctoClawRuntimePlugin {
  name: "octoclaw-runtime-ts";
  createAdapter: () => RuntimeTaskflowAdapter;
  bindWorkflow: (state: RuntimeWorkflowState) => { taskId: string; flowId: string; status: string };
  judgeRoute: (input: PolicyJudgeInput) => PolicyDecision;
}
```

From `extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts`:
```ts
export interface RuntimeTaskflowSessionBinding {
  sessionKey: string;
  bindSession: (sessionKey: string) => RuntimeTaskflowSessionBinding;
  createManaged: (workflow: RuntimeWorkflowState) => { flowId: string; controllerId: string; managed: true };
  runTask: (workflow: RuntimeWorkflowState) => { taskId: string; flowId: string; runtime: "openclaw-native" };
}
```

From `packages/octoclaw-contracts/src/schemas.ts`:
```ts
export type ArtifactKind = "truth" | "projection" | "artifact" | "telemetry";
export interface ScopeMetadata {
  readScope: ScopeDescriptor[];
  writeScope: ScopeDescriptor[];
  workspaceMode: WorkspaceMode;
  writeScopeSummary?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote the TypeScript runtime adapter into the formal native truth seam</name>
  <files>extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts, extensions/octoclaw-runtime/src/plugin.ts, packages/octoclaw-contracts/src/schemas.ts, packages/octoclaw-contracts/src/artifacts.ts, tests/test_openclaw_taskflow_adapter.py</files>
  <read_first>
    <file>extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts</file>
    <file>extensions/octoclaw-runtime/src/plugin.ts</file>
    <file>lib/openclaw_taskflow_runtime_helper.mjs</file>
    <file>lib/openclaw_taskflow_adapter.py</file>
    <file>packages/octoclaw-contracts/src/schemas.ts</file>
    <file>packages/octoclaw-contracts/src/artifacts.ts</file>
    <file>tests/test_openclaw_taskflow_adapter.py</file>
  </read_first>
  <behavior>
    - Test 1: the TS adapter exposes native-truth operations and returns contract fields that distinguish truth from projection per D-02 and D-05.
    - Test 2: adapter outputs carry native task/flow identifiers, sync mode, substrate state/revision, and ownership/workspace metadata needed by downstream surfaces.
    - Test 3: no new contract shape collapses truth, projection, artifact, and telemetry into a single undifferentiated payload.
  </behavior>
  <action>Per D-01, D-02, and D-05, deepen `extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts` from placeholder return values into the formal TypeScript-native execution truth seam. Preserve the existing `bindSession`, `createManaged`, and `runTask` API names, but expand the returned shapes and helper logic so the adapter explicitly models native flow/task identifiers, substrate sync mode, substrate state, substrate revision, managed-vs-mirrored creation disposition, and ownership/controller metadata using the native helper semantics from `lib/openclaw_taskflow_runtime_helper.mjs` as the source reference. Update `extensions/octoclaw-runtime/src/plugin.ts` so `createAdapter()` and `bindWorkflow()` surface this truth-path contract directly. If shared types are needed, add them to `packages/octoclaw-contracts/src/schemas.ts` and/or `packages/octoclaw-contracts/src/artifacts.ts` with explicit truth-vs-projection typing; do not introduce any contract that lets projection or telemetry overwrite truth. Extend `tests/test_openclaw_taskflow_adapter.py` to assert the TS-native seam exposes the required native truth fields and maintains plane separation.</action>
  <acceptance_criteria>
    <criterion>`extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts` contains `bindSession`, `createManaged`, and `runTask` plus explicit native truth fields such as `flowId`, `taskId`, `runtime`, `syncMode`/`sync_mode`, `substrateState`/`substrate_state`, or `revision`.</criterion>
    <criterion>`packages/octoclaw-contracts/src/schemas.ts` and/or `packages/octoclaw-contracts/src/artifacts.ts` still expose distinct `truth`, `projection`, `artifact`, and `telemetry` families.</criterion>
    <criterion>`tests/test_openclaw_taskflow_adapter.py` contains assertions for native binding/truth fields beyond the previous placeholder seam.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_openclaw_taskflow_adapter.py -q</automated>
  </verify>
  <done>The TS adapter is the formal native truth seam, exposes the fields downstream surfaces need, and has passing adapter-focused tests proving the cutover contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire the shipped runtime entrypoint to delegate formal truth-path authority to the TS-native adapter</name>
  <files>extensions/octoclaw-runtime/index.js, extensions/octoclaw-runtime/src/plugin.ts, tests/test_octoclaw_runtime_extension.py</files>
  <read_first>
    <file>extensions/octoclaw-runtime/index.js</file>
    <file>extensions/octoclaw-runtime/src/plugin.ts</file>
    <file>.planning/phases/02-runtime-core-and-safe-delegation/02-03-SUMMARY.md</file>
    <file>.planning/phases/02-runtime-core-and-safe-delegation/02-VERIFICATION.md</file>
    <file>tests/test_octoclaw_runtime_extension.py</file>
  </read_first>
  <behavior>
    - Test 1: the shipped runtime path uses the TypeScript-native adapter/plugin seam as formal authority for native execution truth per D-01.
    - Test 2: the wrapper remains compatibility glue if needed, but does not own a second truth model.
    - Test 3: runtime extension tests observe native-truth fields on the formal path.
  </behavior>
  <action>Per D-01, complete the formal runtime cutover in `extensions/octoclaw-runtime/index.js`. Keep any compatibility wrapper behavior needed for existing extension loading, but move formal truth-path authority to the TypeScript-native plugin/adapter seam rather than the Python adapter or JS-owned truth reconstruction. Concretely: route any native taskflow binding/truth extraction path through `extensions/octoclaw-runtime/src/plugin.ts`, ensure the wrapper records the TS-native truth object on runtime metadata instead of synthesizing a competing authority, and preserve the Phase 2 route guard semantics already enforced by the TS judge. Update `tests/test_octoclaw_runtime_extension.py` so the shipped runtime path proves the native truth delegation is present and observable from the extension-facing surface.</action>
  <acceptance_criteria>
    <criterion>`extensions/octoclaw-runtime/index.js` contains an import or call path into the TS runtime plugin/adapter for formal native truth handling.</criterion>
    <criterion>`extensions/octoclaw-runtime/index.js` does not reintroduce Python adapter authority as the formal truth path.</criterion>
    <criterion>`tests/test_octoclaw_runtime_extension.py` contains assertions that runtime-facing facts include TS-native truth delegation or native binding fields.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_octoclaw_runtime_extension.py -q</automated>
  </verify>
  <done>The shipped runtime entrypoint now delegates formal native truth-path authority to the TypeScript adapter/plugin seam, and extension tests verify the cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime wrapper → TS native adapter | Legacy compatibility code crosses into the new formal truth path |
| TS native adapter → OpenClaw runtime helper/native APIs | Untrusted native/runtime responses enter OctoClaw truth shaping |
| truth contracts → downstream projections | Truth data is transformed into read models that must not overwrite authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts` | mitigate | Validate and normalize native task/flow fields into explicit TS contracts before exposing them downstream; reject ambiguous truth/projection mixing. |
| T-03-02 | S | `extensions/octoclaw-runtime/index.js` | mitigate | Route formal truth authority only through the TS-native plugin seam so compatibility wrapper metadata cannot spoof a second authority path. |
| T-03-03 | I | `packages/octoclaw-contracts/src/*` | mitigate | Preserve explicit `truth` / `projection` / `artifact` / `telemetry` contract families so display payloads cannot leak into live truth mutation. |
| T-03-04 | D | Native helper integration | accept | Native helper failure may temporarily block truth refresh, but Phase 3 can accept fail-closed behavior as long as tests prove the path does not silently fabricate truth. |
</threat_model>

<verification>
- Run `python3 -m pytest tests/test_openclaw_taskflow_adapter.py tests/test_octoclaw_runtime_extension.py -q`.
- Run `node --input-type=module -e "import('./extensions/octoclaw-runtime/src/plugin.ts').then((m)=>{const p=m.createOctoClawRuntimePlugin(); console.log(JSON.stringify({name:p.name,hasAdapter:Boolean(p.createAdapter),hasBind:Boolean(p.bindWorkflow)}));})"`.
</verification>

<success_criteria>
- The formal runtime path uses the TypeScript-native adapter/plugin seam for OpenClaw task/flow truth.
- Shared contracts preserve truth/projection separation while exposing native truth fields needed by downstream surfaces.
- Adapter and runtime extension tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-native-substrate-and-operator-surfaces/03-01-SUMMARY.md`
</output>
