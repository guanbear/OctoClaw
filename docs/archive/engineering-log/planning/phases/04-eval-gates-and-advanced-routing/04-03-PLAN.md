---
phase: 04-eval-gates-and-advanced-routing
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - extensions/octoclaw-runtime/policy/compound_plan.js
  - extensions/octoclaw-runtime/policy/compound_executor.js
  - extensions/octoclaw-runtime/policy/planner.js
  - lib/auto_router.py
  - tests/test_compound_plan.py
  - tests/test_auto_router.py
  - tests/test_auto_router_boundary.py
  - tests/test_eval_suite.py
autonomous: true
requirements:
  - AUTO-01
must_haves:
  truths:
    - "Compound and auto-routing capabilities consume validated runtime and evaluation contracts before any deeper routing execution occurs."
    - "Compound execution degrades safely when lane feasibility, guards, or planner output is invalid."
    - "Auto-router outputs reflect stable policy/runtime facts without introducing a second orchestration authority."
  artifacts:
    - path: "extensions/octoclaw-runtime/policy/compound_plan.js"
      provides: "Validated/scheduled compound-plan contract"
    - path: "extensions/octoclaw-runtime/policy/compound_executor.js"
      provides: "Wave-by-wave gated compound execution ledger"
    - path: "lib/auto_router.py"
      provides: "Auto-router recommendation payload built from internal runtime/policy contracts"
    - path: "tests/test_compound_plan.py"
      provides: "Compound plan validation, scheduling, and executor coverage"
  key_links:
    - from: "extensions/octoclaw-runtime/policy/planner.js"
      to: "extensions/octoclaw-runtime/policy/compound_plan.js"
      via: "normalizeCompoundPlan output for gated compound plans"
      pattern: "normalizeCompoundPlan|decision_mode|compound_plan"
    - from: "extensions/octoclaw-runtime/policy/compound_executor.js"
      to: "extensions/octoclaw-runtime/policy/compound_plan.js"
      via: "validate/schedule/evaluateGuard/validateCompoundExecution"
      pattern: "validateCompoundPlan|scheduleCompoundPlan|evaluateGuard|validateCompoundExecution"
    - from: "lib/auto_router.py"
      to: "tests/test_auto_router.py"
      via: "auto_router recommendation payload"
      pattern: "octoclaw.auto_router.recommendation/v1"
---

<objective>
Enable gated compound routing and auto-router consumption only after eval and shadow contracts are in place, so advanced routing builds on validated runtime facts instead of bypassing them.

Purpose: Complete AUTO-01 with advanced-routing capabilities that remain shadowed or explicitly gated until validation passes.
Output: Hardened compound planner/executor contracts plus auto-router outputs and tests that prove they consume stable policy/runtime facts.
</objective>

<execution_context>
@/Users/guanbear/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/guanbear/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-RESEARCH.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-VALIDATION.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-PATTERNS.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-01-PLAN.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-02-PLAN.md

<interfaces>
From `extensions/octoclaw-runtime/policy/compound_plan.js`:
```js
validateCompoundPlan(plan)
scheduleCompoundPlan(plan)
evaluateGuard(guard, workItemResults)
normalizeCompoundPlan(raw)
validateCompoundExecution(plan, laneFeasibility, runnerStatus)
```

From `extensions/octoclaw-runtime/policy/planner.js`:
```js
invokeCompoundPlanner(...) => { decision_mode: "simple_route" | "compound_plan", plan? }
```

From `lib/auto_router.py` / tests:
```python
payload["schema_version"] == "octoclaw.auto_router.recommendation/v1"
payload["router_core"]
payload["budget_planner"]
payload["adapter"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden compound planner and executor gates before any deeper routing execution</name>
  <files>extensions/octoclaw-runtime/policy/compound_plan.js, extensions/octoclaw-runtime/policy/compound_executor.js, extensions/octoclaw-runtime/policy/planner.js, tests/test_compound_plan.py</files>
  <read_first>
    <file>extensions/octoclaw-runtime/policy/compound_plan.js</file>
    <file>extensions/octoclaw-runtime/policy/compound_executor.js</file>
    <file>extensions/octoclaw-runtime/policy/planner.js</file>
    <file>tests/test_compound_plan.py</file>
    <file>.planning/phases/04-eval-gates-and-advanced-routing/04-02-PLAN.md</file>
  </read_first>
  <acceptance_criteria>
    <criterion>`extensions/octoclaw-runtime/policy/compound_plan.js` still exports `validateCompoundPlan`, `scheduleCompoundPlan`, `evaluateGuard`, `normalizeCompoundPlan`, and `validateCompoundExecution`.</criterion>
    <criterion>`extensions/octoclaw-runtime/policy/compound_executor.js` executes wave-by-wave from validated/scheduled plan data and marks invalid or infeasible work items failed or corrected instead of silently proceeding.</criterion>
    <criterion>`tests/test_compound_plan.py` covers existing schema/schedule behavior plus executor or validation-gate behavior for infeasible lanes, skipped guards, or planner fallback cases.</criterion>
  </acceptance_criteria>
  <action>Use the existing compound-plan stack to formalize Phase 4's gated advanced-routing behavior. Keep `invokeCompoundPlanner()` fail-open to `simple_route` when planners or adapters are unavailable, but once a compound plan exists, ensure `normalizeCompoundPlan()`, `validateCompoundPlan()`, `validateCompoundExecution()`, `scheduleCompoundPlan()`, and `executeCompoundPlan()` are the only route to multi-item execution. Extend the executor or its tests so infeasible lanes, unsupported intents, failed guards, and invalid plan outputs stay visible as corrected/skipped/failed ledger states rather than quietly dispatching broader work. Do not grant compound execution live authority outside these validated gates.</action>
  <verify>
    <automated>python3 -m pytest tests/test_compound_plan.py -q</automated>
  </verify>
  <done>Compound routing is explicitly gated by normalization, validation, feasibility, and guard checks, with regression tests proving safe degradation paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep auto-router outputs bound to stable runtime and evaluation contracts</name>
  <files>lib/auto_router.py, tests/test_auto_router.py, tests/test_auto_router_boundary.py, tests/test_eval_suite.py</files>
  <read_first>
    <file>lib/auto_router.py</file>
    <file>tests/test_auto_router.py</file>
    <file>tests/test_auto_router_boundary.py</file>
    <file>tests/test_eval_suite.py</file>
    <file>.planning/phases/04-eval-gates-and-advanced-routing/04-RESEARCH.md</file>
  </read_first>
  <acceptance_criteria>
    <criterion>`lib/auto_router.py` still emits schema `octoclaw.auto_router.recommendation/v1` and preserves internal-first recommendation payload fields such as `router_core`, `budget_planner`, and `adapter`.</criterion>
    <criterion>`tests/test_auto_router.py` asserts route, work contract, model, and session/route-hint continuity fields come from the internal policy/runtime payload.</criterion>
    <criterion>`tests/test_auto_router_boundary.py` and `tests/test_eval_suite.py` confirm the boundary remains baseline/extractable and eval summaries still count delegated/budget/taskflow facts from existing runtime contracts.</criterion>
  </acceptance_criteria>
  <action>Keep the auto-router surface as a consumer of stable internal policy/runtime/eval contracts rather than a new execution authority. Preserve the existing `octoclaw.auto_router.recommendation/v1` schema and ensure `router_core`, `budget_planner`, `signal`, `adapter`, and model-intel facts all derive from the already-validated decision payload, budget recommendation, and taskflow/eval fields. If boundary metadata or eval summary fields need expansion for Phase 4, express them as concrete read-only payload additions and verify them in `tests/test_auto_router_boundary.py` and `tests/test_eval_suite.py`. Do not introduce any path where auto-router output directly mutates runtime truth or dispatches work.</action>
  <verify>
    <automated>python3 -m pytest tests/test_auto_router.py tests/test_auto_router_boundary.py tests/test_eval_suite.py -q</automated>
  </verify>
  <done>Auto-router remains a contract consumer layered on validated policy/runtime facts, and its boundary/eval tests prove it does not become a second orchestration authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner output → compound plan normalization | External or model-produced plan structure must be validated before use |
| compound execution ledger → dispatch/materialization | Multi-step routing can fan out to runtime operations if gates fail open incorrectly |
| runtime/policy facts → auto-router payload | Read-only recommendation output consumes authoritative runtime facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T | `extensions/octoclaw-runtime/policy/planner.js` | mitigate | Normalize and validate any compound planner result before use, and fail open to `simple_route` when the result is invalid or unavailable. |
| T-04-08 | E | `extensions/octoclaw-runtime/policy/compound_executor.js` | mitigate | Require lane feasibility, guard evaluation, and scheduled waves before dispatch/materialization; mark invalid paths skipped/failed instead of executing them. |
| T-04-09 | I | `lib/auto_router.py` | mitigate | Keep auto-router payloads read-only and sourced from validated decision/runtime/eval contracts so recommendation output cannot overwrite execution truth. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_compound_plan.py tests/test_auto_router.py tests/test_auto_router_boundary.py tests/test_eval_suite.py -q`
</verification>

<success_criteria>
- Compound routing executes only from validated plan and feasibility contracts.
- Auto-router outputs remain schema-stable and read-only.
- Focused advanced-routing tests pass after the eval/shadow groundwork is in place.
</success_criteria>

<output>
After completion, create `.planning/phases/04-eval-gates-and-advanced-routing/04-03-SUMMARY.md`
</output>
