---
phase: 04-eval-gates-and-advanced-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/replay_validation.py
  - lib/harness_gate.py
  - lib/acceptance_runtime.py
  - tests/test_replay_validation.py
  - tests/test_delivery_relay_reconcile.py
  - tests/test_harness_gate.py
  - tests/test_acceptance_runtime.py
autonomous: true
requirements:
  - EVAL-01
must_haves:
  truths:
    - "Replay, relay, and acceptance gates catch duplicate delivery or stale recovery regressions before promotion."
    - "Black-box acceptance bootstrap runs from workspace-local temp roots with reproducible config and agent files."
    - "Gate presets explicitly include the eval modules required for promotion."
  artifacts:
    - path: "lib/replay_validation.py"
      provides: "Replay validation and route-outcome summarization used by promotion gates"
    - path: "lib/harness_gate.py"
      provides: "Quick/full gate preset selection for eval and acceptance runs"
    - path: "lib/acceptance_runtime.py"
      provides: "Acceptance environment bootstrap rooted in workspace-local temp directories"
    - path: "tests/test_harness_gate.py"
      provides: "Regression checks for quick/full promotion presets"
  key_links:
    - from: "lib/harness_gate.py"
      to: "tests/test_harness_gate.py"
      via: "preset JSON listing"
      pattern: "preset|modules|full|quick"
    - from: "lib/replay_validation.py"
      to: "tests/test_replay_validation.py"
      via: "route outcome coverage and safety filtering"
      pattern: "coverage_complete|delivery_correct_cases|fallback_taken_count"
    - from: "lib/acceptance_runtime.py"
      to: "tests/test_acceptance_runtime.py"
      via: "workspace-rooted acceptance bootstrap"
      pattern: "octoclaw-acceptance|gateway|target_workspace"
---

<objective>
Make replay, delivery, harness, and black-box acceptance checks promotion-grade so Phase 4 can catch runtime regressions before advanced routing is allowed to deepen.

Purpose: Satisfy EVAL-01 by putting duplicate-delivery, stale recovery, and acceptance bootstrap checks on the critical verification path.
Output: Stronger eval gate helpers and focused regression coverage for replay validation, delivery relay reconciliation, harness presets, and acceptance bootstrap.
</objective>

<execution_context>
@/Users/guanbear/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/guanbear/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-RESEARCH.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-VALIDATION.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-PATTERNS.md
@.planning/phases/03-native-substrate-and-operator-surfaces/03-VERIFICATION.md

<interfaces>
From `lib/replay_validation.py` / tests:
```python
summarize_route_outcomes(cases) -> {
  "cases_total": int,
  "coverage_complete": bool,
  "route_correct_cases": int,
  "budget_correct_cases": int,
  "delivery_correct_cases": int,
  "fallback_taken_count": int,
}
```

From `lib/harness_gate.py` / tests:
```text
python3 lib/harness_gate.py --preset quick --list --format json
python3 lib/harness_gate.py --preset full --list --format json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen replay and delivery gate summaries around duplicate, stale, and fallback outcomes</name>
  <files>lib/replay_validation.py, tests/test_replay_validation.py, tests/test_delivery_relay_reconcile.py</files>
  <read_first>
    <file>lib/replay_validation.py</file>
    <file>tests/test_replay_validation.py</file>
    <file>tests/test_delivery_relay_reconcile.py</file>
    <file>.planning/phases/03-native-substrate-and-operator-surfaces/03-VERIFICATION.md</file>
    <file>.planning/phases/04-eval-gates-and-advanced-routing/04-RESEARCH.md</file>
  </read_first>
  <acceptance_criteria>
    <criterion>`tests/test_replay_validation.py` contains assertions for route coverage plus delivery-related counts such as `delivery_correct_cases` and `fallback_taken_count`.</criterion>
    <criterion>`lib/replay_validation.py` continues to return `coverage_complete` and includes route/budget/delivery correctness counts in the summary payload.</criterion>
    <criterion>`tests/test_delivery_relay_reconcile.py` still proves pending deliveries are compensated, deferred, or left pending without silent success.</criterion>
  </acceptance_criteria>
  <action>Update the replay-validation path so Phase 4 gate summaries make duplicate-delivery, stale recovery, fallback, and route/budget correctness visible in one promotion-oriented payload instead of only surfacing route correctness. Keep the existing `summarize_route_outcomes()` contract JSON-friendly; extend it only with concrete counters or flags already derivable from replay events and delivery outcomes. Add or refine tests so replay packets with mismatched execution contracts, failed delivery retries, or unresolved fallback paths are counted explicitly and remain grep/assert visible in the summary structure. Do not move this data into runtime truth; keep it as replay/eval output per RT-02 and EVAL-01.</action>
  <verify>
    <automated>python3 -m pytest tests/test_replay_validation.py tests/test_delivery_relay_reconcile.py -q</automated>
  </verify>
  <done>Replay and delivery gate summaries visibly report the regression classes Phase 4 must catch, and focused tests prove those summaries stay accurate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote harness presets and acceptance bootstrap into the eval gate path</name>
  <files>lib/harness_gate.py, lib/acceptance_runtime.py, tests/test_harness_gate.py, tests/test_acceptance_runtime.py</files>
  <read_first>
    <file>lib/harness_gate.py</file>
    <file>lib/acceptance_runtime.py</file>
    <file>tests/test_harness_gate.py</file>
    <file>tests/test_acceptance_runtime.py</file>
    <file>.planning/phases/04-eval-gates-and-advanced-routing/04-VALIDATION.md</file>
  </read_first>
  <acceptance_criteria>
    <criterion>`tests/test_harness_gate.py` lists Phase 4 eval modules in the quick/full presets, including router-policy, shadow-report, runtime-extension, and acceptance-related coverage where applicable.</criterion>
    <criterion>`lib/acceptance_runtime.py` continues to bootstrap acceptance paths under workspace-local `tmp/` roots and configures `gateway.port` and `gateway.bind` deterministically.</criterion>
    <criterion>`tests/test_acceptance_runtime.py` asserts copied config/agent files and workspace temp directories exist after bootstrap.</criterion>
  </acceptance_criteria>
  <action>Update the harness preset definitions so the quick and full gate lists explicitly cover the Phase 4 eval modules called out in the validation strategy: route-policy goldens, shadow reporting, runtime-extension checks, replay validation, and acceptance bootstrap. Keep the CLI JSON list contract stable. In `lib/acceptance_runtime.py`, preserve the workspace-local temp-root behavior and deterministic gateway setup; only add the concrete file-copying or bootstrap guarantees needed to support promotion-grade black-box acceptance runs. Reflect the exact module and bootstrap expectations in the paired tests so the promotion path stays machine-verifiable.</action>
  <verify>
    <automated>python3 -m pytest tests/test_harness_gate.py tests/test_acceptance_runtime.py -q</automated>
  </verify>
  <done>Harness presets and acceptance bootstrap form a reliable promotion gate path, and focused tests prove the right modules and runtime scaffolding are present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay logs → eval summaries | Historical runtime artifacts are interpreted into promotion decisions |
| delivery relay events → reconciliation gate | Unreconciled delivery state influences whether the gate passes |
| acceptance bootstrap → temp runtime | Copied config and agent files form an isolated black-box runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `lib/replay_validation.py` | mitigate | Derive route/delivery/fallback counts from recorded replay/event payloads only, and keep summary outputs explicit so regressions cannot be hidden by silent aggregation. |
| T-04-02 | R | `tests/test_delivery_relay_reconcile.py` | mitigate | Preserve explicit assertions for compensated, deferred, and task-not-ready states so duplicate or stale relay paths remain auditable. |
| T-04-03 | E | `lib/acceptance_runtime.py` | mitigate | Keep acceptance bootstrap rooted in workspace-local temp directories with deterministic gateway bind/port values instead of inheriting uncontrolled host state. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_replay_validation.py tests/test_delivery_relay_reconcile.py tests/test_harness_gate.py tests/test_acceptance_runtime.py -q`
</verification>

<success_criteria>
- Promotion-oriented replay and delivery summaries expose duplicate/fallback/stale failure classes.
- Harness presets explicitly include the eval modules needed for Phase 4 promotion.
- Acceptance bootstrap remains reproducible from workspace-local temp roots.
</success_criteria>

<output>
After completion, create `.planning/phases/04-eval-gates-and-advanced-routing/04-01-SUMMARY.md`
</output>
