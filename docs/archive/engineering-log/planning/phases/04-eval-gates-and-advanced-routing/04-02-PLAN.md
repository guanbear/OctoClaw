---
phase: 04-eval-gates-and-advanced-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/octoclaw-runtime/policy/decide.js
  - extensions/octoclaw-runtime/policy/recommendation.js
  - extensions/octoclaw-runtime/policy/config.js
  - extensions/octoclaw-runtime/index.js
  - tests/test_router_policy_v2_goldens.py
  - tests/test_policy_judge_shadow_report.py
  - tests/test_route_recommendation.py
  - tests/test_octoclaw_runtime_extension.py
autonomous: true
requirements:
  - EVAL-01
  - AUTO-01
must_haves:
  truths:
    - "Route-policy goldens and shadow outputs remain valid under the runtime contract."
    - "Optimization telemetry is available at route/request level without becoming execution truth."
    - "Replay events expose route recommendation conflicts and judge/shadow evidence for promotion analysis."
  artifacts:
    - path: "extensions/octoclaw-runtime/policy/recommendation.js"
      provides: "Route and budget recommendation contracts used as non-authoritative optimization telemetry"
    - path: "extensions/octoclaw-runtime/policy/decide.js"
      provides: "Route-policy decisions that emit route_recommendation, budget_recommendation, and auto_router payloads"
    - path: "tests/test_policy_judge_shadow_report.py"
      provides: "Shadow drift summary coverage"
    - path: "tests/test_router_policy_v2_goldens.py"
      provides: "Golden route-policy contract coverage"
  key_links:
    - from: "extensions/octoclaw-runtime/policy/decide.js"
      to: "extensions/octoclaw-runtime/policy/recommendation.js"
      via: "buildRouteRecommendation and buildBudgetRecommendation"
      pattern: "buildRouteRecommendation|buildBudgetRecommendation"
    - from: "extensions/octoclaw-runtime/index.js"
      to: "tests/test_octoclaw_runtime_extension.py"
      via: "replay payloads and runtime metadata"
      pattern: "policy_resolved|route_validated|routeRecommendationConflict"
    - from: "tests/test_router_policy_v2_goldens.py"
      to: "tests/fixtures/router-policy-goldens-v2.json"
      via: "golden contract expectations"
      pattern: "router_schema|request_kind|scope|target"
---

<objective>
Stabilize route-policy goldens, shadow reporting, and optimization telemetry so routing deepening can compare recommendations to executed behavior without bypassing runtime truth.

Purpose: Cover EVAL-01 and AUTO-01 by making route-policy drift and optimization signals measurable, explicit, and non-authoritative.
Output: Updated recommendation/replay contracts plus golden and shadow tests that validate them.
</objective>

<execution_context>
@/Users/guanbear/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/guanbear/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-RESEARCH.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-VALIDATION.md
@.planning/phases/04-eval-gates-and-advanced-routing/04-PATTERNS.md
@.planning/phases/03-native-substrate-and-operator-surfaces/03-VERIFICATION.md

<interfaces>
From `extensions/octoclaw-runtime/policy/recommendation.js`:
```js
buildBudgetRecommendation(...) => { schema_version: "octoclaw.budget_recommendation/v1", ... }
buildRouteRecommendation(...) => { schema_version: "octoclaw.route_recommendation/v1", arbitration: {...} }
```

From `extensions/octoclaw-runtime/policy/decide.js`:
```js
decision.route_recommendation
decision.budget_recommendation
decision.auto_router
decision.router_decision_v2
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten route-policy golden contracts and recommendation telemetry fields</name>
  <files>extensions/octoclaw-runtime/policy/decide.js, extensions/octoclaw-runtime/policy/recommendation.js, extensions/octoclaw-runtime/policy/config.js, tests/test_router_policy_v2_goldens.py, tests/test_route_recommendation.py</files>
  <read_first>
    <file>extensions/octoclaw-runtime/policy/decide.js</file>
    <file>extensions/octoclaw-runtime/policy/recommendation.js</file>
    <file>extensions/octoclaw-runtime/policy/config.js</file>
    <file>tests/test_router_policy_v2_goldens.py</file>
    <file>tests/test_route_recommendation.py</file>
    <file>.planning/phases/04-eval-gates-and-advanced-routing/04-RESEARCH.md</file>
  </read_first>
  <acceptance_criteria>
    <criterion>`extensions/octoclaw-runtime/policy/decide.js` still emits `route_recommendation`, `budget_recommendation`, and `auto_router` on the decision payload.</criterion>
    <criterion>`extensions/octoclaw-runtime/policy/recommendation.js` continues to return schema versions `octoclaw.route_recommendation/v1` and `octoclaw.budget_recommendation/v1` and exposes arbitration / consistency metadata.</criterion>
    <criterion>`tests/test_router_policy_v2_goldens.py` and `tests/test_route_recommendation.py` assert contract-valid recommendation fields without treating them as execution truth.</criterion>
  </acceptance_criteria>
  <action>Refine the recommendation and policy-decision contracts so Phase 4 can compare executed routes to recommended routes, worker pools, work contracts, and budget consistency without altering live authority. Keep `route_recommendation`, `budget_recommendation`, and `auto_router` attached to the decision payload in `extensions/octoclaw-runtime/policy/decide.js`. In `recommendation.js`, preserve the existing schema versions and extend or clarify arbitration/conflict/consistency fields only as explicit recommendation telemetry. If config gating is needed, place it in `policy/config.js` with concrete keys under the existing runtime policy structure instead of ad hoc environment-only behavior. Update the golden and recommendation tests to assert these fields concretely and to keep unknown or inconsistent routes visibly non-consistent rather than silently green.</action>
  <verify>
    <automated>python3 -m pytest tests/test_router_policy_v2_goldens.py tests/test_route_recommendation.py -q</automated>
  </verify>
  <done>Route-policy decisions emit stable recommendation telemetry and golden tests prove those contracts stay valid and non-authoritative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose shadow drift and replay recommendation evidence on the runtime path</name>
  <files>extensions/octoclaw-runtime/index.js, tests/test_policy_judge_shadow_report.py, tests/test_octoclaw_runtime_extension.py</files>
  <read_first>
    <file>extensions/octoclaw-runtime/index.js</file>
    <file>tests/test_policy_judge_shadow_report.py</file>
    <file>tests/test_octoclaw_runtime_extension.py</file>
    <file>.planning/phases/03-native-substrate-and-operator-surfaces/03-VERIFICATION.md</file>
  </read_first>
  <acceptance_criteria>
    <criterion>`extensions/octoclaw-runtime/index.js` replay payload builders still emit fields such as `routeRecommendationConflict`, `routeRecommendationStrategy`, and `routeRecommendationConflictType`.</criterion>
    <criterion>`tests/test_policy_judge_shadow_report.py` asserts shadow compared/matched/drifted counts from a report payload with schema `octoclaw.policy_judge.shadow_report/v1`.</criterion>
    <criterion>`tests/test_octoclaw_runtime_extension.py` contains targeted assertions that replay- or decision-level metadata preserves recommendation conflict or policy-judge shadow evidence without overwriting `runtime_truth`.</criterion>
  </acceptance_criteria>
  <action>Keep the runtime wrapper's replay and metadata path observational: use `extensions/octoclaw-runtime/index.js` to record recommendation conflict state, judge/shadow invocation details, cache state, and route-validation outcomes on replay payloads, but do not merge any of that data into `runtime_truth`. Where Phase 3 left broader route-policy failures deferred, convert the Phase 4-critical parts into explicit targeted tests or assertions so drift becomes visible in shadow reporting instead of hidden in the full suite. Preserve the existing shadow report schema and summary shape, and ensure targeted runtime extension checks can prove the replay path carries recommendation conflict evidence alongside the authoritative truth payload rather than inside it.</action>
  <verify>
    <automated>python3 -m pytest tests/test_policy_judge_shadow_report.py tests/test_octoclaw_runtime_extension.py -q -k "policy_judge or replay or runtime_wrapper"</automated>
  </verify>
  <done>Route-policy shadow drift and recommendation conflicts are measurable from replay and targeted runtime tests, while runtime truth remains separate and authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy decision → recommendation telemetry | Non-authoritative routing advice is attached to executable decisions |
| runtime wrapper → replay logs | Runtime metadata is recorded for later drift analysis |
| golden fixtures → contract verification | Fixture expectations guard route-policy stability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | I | `extensions/octoclaw-runtime/policy/decide.js` | mitigate | Keep recommendation, budget, and auto-router payloads on observational decision fields and do not merge them into `runtime_truth`. |
| T-04-05 | R | `extensions/octoclaw-runtime/index.js` | mitigate | Preserve replay payload fields for recommendation conflicts, judge invocation state, and validation outcomes so routing drift remains auditable after execution. |
| T-04-06 | T | golden/shadow fixtures and tests | mitigate | Assert exact schema/version and summary fields in targeted tests so silent contract drift cannot pass promotion gates. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_router_policy_v2_goldens.py tests/test_route_recommendation.py tests/test_policy_judge_shadow_report.py -q`
- `python3 -m pytest tests/test_octoclaw_runtime_extension.py -q -k "policy_judge or replay or runtime_wrapper"`
</verification>

<success_criteria>
- Recommendation, budget, and auto-router contracts remain schema-stable and non-authoritative.
- Replay/shadow outputs visibly report routing drift and recommendation conflicts.
- Targeted route-policy goldens and shadow tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-eval-gates-and-advanced-routing/04-02-SUMMARY.md`
</output>
