---
phase: 03-native-substrate-and-operator-surfaces
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - extensions/octoclaw-runtime/conversation-control.js
  - lib/im_display_contract.py
  - lib/task_display.py
  - tests/test_octoclaw_runtime_extension.py
  - tests/test_im_thread.py
autonomous: true
requirements:
  - SURF-01
must_haves:
  truths:
    - "IM/display surfaces consume the same shared projection contracts as operator surfaces."
    - "Conversation grounding and IM task surfaces no longer own a separate truth model."
    - "IM display contracts explicitly forbid renderer-authored or unconfirmed truth fields."
  artifacts:
    - path: "extensions/octoclaw-runtime/conversation-control.js"
      provides: "IM grounding and follow-up fact presentation backed by shared projections"
    - path: "lib/im_display_contract.py"
      provides: "Shared IM/display contract aligned to substrate-first projection fields"
    - path: "tests/test_octoclaw_runtime_extension.py"
      provides: "Regression coverage for IM/runtime grounding on shared projections"
  key_links:
    - from: "extensions/octoclaw-runtime/conversation-control.js"
      to: "lib/im_display_contract.py"
      via: "shared projection/display vocabulary"
      pattern: "substrate|queue|task_id|state|action_availability"
    - from: "extensions/octoclaw-runtime/conversation-control.js"
      to: "lib/task_display.py"
      via: "shared task surface / projection-backed fields"
      pattern: "task anchor|timeline|queue|details"
---

<objective>
Rebind IM and display consumers to the shared substrate projection contracts.

Purpose: Satisfy D-04 by ensuring conversation grounding and IM/display surfaces consume the same execution-read model as operator views instead of creating their own truth.
Output: Updated IM/display contracts and runtime follow-up grounding that reuse the shared substrate projection vocabulary with regression coverage.
</objective>

<execution_context>
@/Users/guanbear/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/guanbear/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-substrate-and-operator-surfaces/03-CONTEXT.md
@.planning/phases/03-native-substrate-and-operator-surfaces/03-RESEARCH.md
@.planning/phases/03-native-substrate-and-operator-surfaces/03-VALIDATION.md
@.planning/phases/03-native-substrate-and-operator-surfaces/03-02-PLAN.md

<interfaces>
From `extensions/octoclaw-runtime/conversation-control.js`:
```js
export function buildConversationGrounding({ prompt = "", replayLogPath = "", taskStatePath = "", sessionKeys = [] } = {})
```

From `lib/im_display_contract.py`:
```py
SUBSTRATE_DISPLAY_CONTRACT = {
  "required_fields": ["task_id", "state", "route", "worker_pool", "substrate_summary", "action_availability"],
  "forbidden_inferred_fields": ["guessed_task_state", "renderer_authored_truth", "unconfirmed_delivery_state"],
}
```

From `lib/task_display.py`:
```py
def build_operator_task_surface(task: dict[str, Any], *, now: datetime | None = None) -> dict[str, Any]
def build_user_task_surface(task: dict[str, Any], *, now: datetime | None = None) -> dict[str, Any]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align IM/display contract definitions to the shared substrate projection vocabulary</name>
  <files>lib/im_display_contract.py, lib/task_display.py, tests/test_im_thread.py</files>
  <read_first>
    <file>lib/im_display_contract.py</file>
    <file>lib/task_display.py</file>
    <file>tests/test_im_thread.py</file>
    <file>.planning/phases/03-native-substrate-and-operator-surfaces/03-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: IM/display contract required fields match the shared substrate-first projection vocabulary from D-03 and D-04.
    - Test 2: task surfaces exposed to IM consumers reuse the same anchor/projection fields rather than introducing separate truth-only fields.
    - Test 3: forbidden inferred fields remain forbidden and are not surfaced by IM task payloads.
  </behavior>
  <action>Per D-04 and D-05, update `lib/im_display_contract.py` so the shared IM/display contract explicitly aligns with the substrate-first projection vocabulary established in Plan 03-02, including ownership/workspace/substrate/action-availability expectations where appropriate. Update `lib/task_display.py` only as needed so IM-facing task surfaces (`build_operator_task_surface`, `build_user_task_surface`, anchor text payloads, or related helpers) expose that shared contract rather than a separate IM-specific truth model. Extend `tests/test_im_thread.py` to verify IM-facing payloads still render correctly while reusing the shared projection vocabulary and avoiding forbidden inferred fields.</action>
  <acceptance_criteria>
    <criterion>`lib/im_display_contract.py` defines required/optional/forbidden fields consistent with the shared substrate projection vocabulary.</criterion>
    <criterion>`lib/task_display.py` IM-facing surfaces reuse projection-backed task anchor fields.</criterion>
    <criterion>`tests/test_im_thread.py` asserts IM task payload/text still renders and reflects the shared contract.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_im_thread.py -q</automated>
  </verify>
  <done>The IM/display contract is aligned to the shared substrate projection vocabulary and IM rendering tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make conversation grounding consume shared substrate projections instead of owning separate truth reconstruction</name>
  <files>extensions/octoclaw-runtime/conversation-control.js, tests/test_octoclaw_runtime_extension.py</files>
  <read_first>
    <file>extensions/octoclaw-runtime/conversation-control.js</file>
    <file>extensions/octoclaw-runtime/index.js</file>
    <file>lib/im_display_contract.py</file>
    <file>lib/runtime_snapshot.py</file>
    <file>lib/task_display.py</file>
    <file>tests/test_octoclaw_runtime_extension.py</file>
  </read_first>
  <behavior>
    - Test 1: `buildConversationGrounding()` reports execution facts through the shared substrate projection vocabulary instead of bespoke truth ownership.
    - Test 2: IM follow-up answers still refuse to guess from memory, but now cite shared projection facts for status, queue, delivery, ownership, and native binding state.
    - Test 3: runtime extension tests prove the IM/read path and operator/read path are aligned on the same projection truth.
  </behavior>
  <action>Update `extensions/octoclaw-runtime/conversation-control.js` so `buildConversationGrounding()` and related follow-up fact assembly consume the shared substrate projection vocabulary introduced in Plan 03-02, instead of reconstructing a separate IM truth model from replay/task/event fragments alone. Preserve the current safety behavior — do not guess from memory, and state plainly when facts are unavailable — but source status, queue, delivery, native binding, and ownership/workspace display facts from the shared projection contract wherever available. Extend `tests/test_octoclaw_runtime_extension.py` to assert that grounded follow-up context contains the shared projection-backed fields and remains aligned with the operator surface contract.</action>
  <acceptance_criteria>
    <criterion>`extensions/octoclaw-runtime/conversation-control.js` references shared projection-backed facts for task state rather than only bespoke local reconstruction.</criterion>
    <criterion>`tests/test_octoclaw_runtime_extension.py` asserts grounded follow-up output includes projection-backed status/queue/native-binding facts.</criterion>
    <criterion>No code path introduces a separate IM-owned truth field family that conflicts with `SUBSTRATE_DISPLAY_CONTRACT` forbidden fields.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_octoclaw_runtime_extension.py tests/test_im_thread.py -q</automated>
  </verify>
  <done>Conversation grounding and IM/display surfaces consume the same shared projection contracts as operator surfaces, with passing regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared substrate projections → IM grounding | User-facing IM context consumes execution facts for conversational follow-up |
| display contract → IM renderer payloads | Contract enforcement prevents renderer-authored truth from leaking into user-visible state |
| operator/read path ↔ IM/read path | Two presentation paths must stay aligned on one truth-derived projection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | I | `extensions/octoclaw-runtime/conversation-control.js` | mitigate | Build grounded follow-up context from shared substrate projection facts and preserve explicit “fact unavailable” behavior instead of guessing. |
| T-03-06 | T | `lib/im_display_contract.py` | mitigate | Keep forbidden inferred fields explicit and verify IM/display payloads do not introduce renderer-authored truth or unconfirmed delivery state. |
| T-03-07 | R | IM vs operator surfaces | mitigate | Add regression tests ensuring grounded follow-up fields match the shared projection vocabulary used by task display/operator surfaces. |
| T-03-08 | S | IM action/display payloads | accept | IM channels have varying capability levels; Phase 3 accepts capability-specific presentation differences as long as the underlying projection facts remain shared and read-only. |
</threat_model>

<verification>
- Run `python3 -m pytest tests/test_octoclaw_runtime_extension.py tests/test_im_thread.py -q`.
- Confirm grounded follow-up output still refuses memory-based answering while citing projection-backed state fields.
</verification>

<success_criteria>
- IM/display contracts align to the same shared substrate projection vocabulary as operator surfaces.
- Conversation grounding no longer owns a separate truth model.
- IM/runtime regression tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-native-substrate-and-operator-surfaces/03-03-SUMMARY.md`
</output>
