---
phase: 05-execution-substrate-refactor
plan: 01
type: execute
wave: 1
depends_on:
  - 02-02
  - 03-01
  - 04-03
files_modified:
  - packages/octoclaw-contracts/src/schemas.ts
  - packages/octoclaw-contracts/src/artifacts.ts
  - packages/octoclaw-runtime-core/src/workflow/index.ts
  - packages/octoclaw-runtime-core/src/tasks/
  - extensions/octoclaw-runtime/src/plugin.ts
  - extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts
  - tests/
autonomous: true
requirements:
  - RT-02
must_haves:
  truths:
    - "Execution orchestration and lifecycle truth are formal TS contracts, not script-shaped compatibility payloads."
    - "The orchestrator/lifecycle model follows the canonical Execution/Workflow Plane and Truth/Projection/Artifact/Telemetry separation from the 2026-04-15 design docs."
    - "The first refactor step defines new formal authorities before replacing the remaining Python/sh live path."
  artifacts:
    - path: "packages/octoclaw-contracts/src/schemas.ts"
      provides: "Formal execution identity, lifecycle, and substrate contract vocabulary"
    - path: "packages/octoclaw-contracts/src/artifacts.ts"
      provides: "Typed task packet, lifecycle artifact, and plane-separated execution payload contracts"
    - path: "packages/octoclaw-runtime-core/src/workflow/index.ts"
      provides: "Execution orchestrator and lifecycle store interfaces aligned to the TS rebuild design"
  key_links:
    - from: "packages/octoclaw-policy/src/judge/index.ts"
      to: "packages/octoclaw-runtime-core/src/workflow/index.ts"
      via: "validated route/role/backend/profile output feeding formal execution orchestration"
      pattern: "PolicyDecision|route|role|backend|modelProfile"
    - from: "packages/octoclaw-runtime-core/src/workflow/index.ts"
      to: "extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts"
      via: "native task/flow materialization and truth binding"
      pattern: "bindSession|createManaged|runTask|truth|projection|artifact|telemetry"
---

<objective>
Define the first formal execution-substrate contracts for the TS rebuild: a real execution orchestrator and lifecycle store vocabulary that match the 2026-04-15 canonical design, instead of inheriting old Python script boundaries.

Purpose: Create the typed contract foundation that the rest of Phase 5 will implement against, so replacing Python/sh live-path authorities becomes an architecture-driven cutover rather than a script-for-script rewrite.
Output: Updated TS contracts and runtime-core workflow interfaces for execution identity, lifecycle truth, materialization, checkpoints, claims, heartbeats, delivery, and provenance.
</objective>

<context>
- `origin/codex/docs-ts-rebuild-plan-2026-04-15:docs/octoclaw-ts-rebuild-design-v1.md`
- `origin/codex/docs-ts-rebuild-plan-2026-04-15:docs/octoclaw-ts-rebuild-implementation-plan-2026-04-15.md`
- `.planning/phases/05-execution-substrate-refactor/05-CONTEXT.md`
- `.planning/phases/05-execution-substrate-refactor/05-PLAN.md`
- `.planning/phases/02-runtime-core-and-safe-delegation/02-02-SUMMARY.md`
- `.planning/phases/03-native-substrate-and-operator-surfaces/03-01-PLAN.md`
- `packages/octoclaw-contracts/src/schemas.ts`
- `packages/octoclaw-contracts/src/artifacts.ts`
- `packages/octoclaw-runtime-core/src/workflow/index.ts`
- `extensions/octoclaw-runtime/src/plugin.ts`
- `extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts`
</context>

<interfaces>
Current policy output:
```ts
interface PolicyDecision {
  route: LiveRoute
  role: PolicyRole
  backend: BackendTarget
  workspaceMode: WorkspaceMode
  modelProfile: ModelProfile
  admission: AdmissionDecision
}
```

Current runtime seam:
```ts
interface RuntimeWorkflowState {
  requestId: string
  taskId: string
  flowId: string
  deadlines: RuntimeDeadlines
  claim: RuntimeClaim | null
  outbox: DeliveryOutbox
  ackLedger: AckLedger
  scope: ScopeMetadata
  taskMaterialization: RuntimeTaskMaterialization
}
```

Target direction from canonical docs:
```text
Interaction / Policy Plane
Execution / Workflow Plane
Evaluation / Learning Plane

truth / projection / artifact / telemetry separation
plugin/runtime-first native task/flow truth
```
</interfaces>

<tasks>

<task type="design+execute" tdd="true">
  <name>Task 1: Replace script-shaped workflow state with formal execution orchestrator contracts</name>
  <files>packages/octoclaw-contracts/src/schemas.ts, packages/octoclaw-runtime-core/src/workflow/index.ts, tests/</files>
  <read_first>
    <file>packages/octoclaw-policy/src/judge/index.ts</file>
    <file>packages/octoclaw-runtime-core/src/workflow/index.ts</file>
    <file>origin/codex/docs-ts-rebuild-plan-2026-04-15:docs/octoclaw-ts-rebuild-design-v1.md</file>
    <file>origin/codex/docs-ts-rebuild-plan-2026-04-15:docs/octoclaw-ts-rebuild-implementation-plan-2026-04-15.md</file>
  </read_first>
  <action>Redefine `RuntimeWorkflowState` and adjacent runtime-core workflow interfaces so they model the canonical execution/workflow plane instead of mirroring old script responsibilities. Introduce explicit execution-orchestrator concepts such as execution authority, materialization intent, lifecycle phase, ownership/lease state, checkpoint status, delivery state, and provenance source. The result must consume `PolicyDecision` as its upstream route authority and must remain substrate-first and plane-separated. Avoid preserving old Python CLI argument shapes as the new contract vocabulary.</action>
  <acceptance_criteria>
    <criterion>`packages/octoclaw-runtime-core/src/workflow/index.ts` exposes a formal execution orchestrator/lifecycle store vocabulary beyond the current minimal queued/running state model.</criterion>
    <criterion>Lifecycle contracts include explicit fields for execution authority, ownership/lease, materialization status, and delivery/provenance state.</criterion>
    <criterion>Tests verify that runtime-core contracts are driven by typed policy output rather than script-specific payload assumptions.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests -q -k "runtime_core or taskflow or octoclaw_runtime_extension"</automated>
  </verify>
  <done>The runtime-core workflow interfaces now define a real execution orchestrator contract that later Phase 5 plans can implement against.</done>
</task>

<task type="design+execute" tdd="true">
  <name>Task 2: Expand contracts for native lifecycle truth and execution packets</name>
  <files>packages/octoclaw-contracts/src/schemas.ts, packages/octoclaw-contracts/src/artifacts.ts, extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts, tests/</files>
  <read_first>
    <file>packages/octoclaw-contracts/src/schemas.ts</file>
    <file>packages/octoclaw-contracts/src/artifacts.ts</file>
    <file>extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts</file>
    <file>origin/codex/docs-ts-rebuild-plan-2026-04-15:docs/octoclaw-ts-rebuild-design-v1.md</file>
  </read_first>
  <action>Extend contracts so execution identity, task packet acceptance, lifecycle events, and native substrate truth are first-class TS artifacts. Keep truth/projection/artifact/telemetry separated, but add the formal packet and lifecycle vocabulary that Phase 5 will need to replace `dispatch_task.py`, `octoclaw_spawn.py`, and `task-state-update.py`. Prioritize new execution semantics over backward-looking JSON/file compatibility fields. Ensure `runtime-taskflow.ts` can bind to these new contracts without inventing a second substrate truth family.</action>
  <acceptance_criteria>
    <criterion>`packages/octoclaw-contracts/src/schemas.ts` includes explicit execution/lifecycle identity concepts needed by orchestrator, runner, and spawn substrates.</criterion>
    <criterion>`packages/octoclaw-contracts/src/artifacts.ts` includes formal task packet / lifecycle artifact contracts beyond minimal brief/result placeholders.</criterion>
    <criterion>`extensions/octoclaw-runtime/src/adapter/runtime-taskflow.ts` can project native truth into the expanded contracts without collapsing truth/projection/artifact/telemetry planes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests -q -k "openclaw_taskflow_adapter or octoclaw_runtime_extension or contracts"</automated>
  </verify>
  <done>The TS contracts are rich enough to support a native execution substrate refactor without inheriting old script/module shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy output -> execution orchestrator contracts | Validated route authority becomes formal execution intent |
| runtime-core lifecycle store -> native taskflow adapter | Orchestrator state is materialized through substrate truth without a second authority path |
| truth contracts -> downstream projection/artifact consumers | Richer execution contracts must not blur truth with display or telemetry payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | T | runtime-core workflow contracts | mitigate | Define new execution/lifecycle vocabulary from canonical architecture instead of preserving legacy script argument surfaces. |
| T-05-01-02 | I | contract plane separation | mitigate | Keep truth/projection/artifact/telemetry families explicit while expanding lifecycle/task packet semantics. |
| T-05-01-03 | E | native adapter alignment | mitigate | Ensure runtime-taskflow adapter binds to new lifecycle contracts without becoming a second orchestrator or truth owner. |
</threat_model>

<verification>
- Contract-focused tests covering runtime-core workflow state, task packet/lifecycle artifacts, and native taskflow adapter alignment.
- Review that no newly introduced formal contract is shaped around Python CLI payloads or shell wrapper assumptions.
</verification>

<success_criteria>
- The first Phase 5 implementation step defines a canonical execution orchestrator and lifecycle contract model.
- TS contracts and runtime-core interfaces are ready for later plans to replace live Python/sh authorities without architectural backtracking.
</success_criteria>

<output>
After completion, create `.planning/phases/05-execution-substrate-refactor/05-01-SUMMARY.md`.
</output>
